- [ ] 解决方案产出总清单（可交付）
  - [ ] 统一输入与边界
    - [ ] 明确业务目标与成功指标（KPI/验收口径）
    - [ ] 明确用户/角色与使用场景（Who/When/Where）
    - [ ] 明确范围（In scope / Out of scope）
    - [ ] 明确约束（成本/时延/合规/数据驻留/依赖）
    - [ ] 明确假设与风险清单（可验证假设）

  - [ ] 需求分析（通用）
    - [ ] 功能需求列表（Must/Should/Could）
    - [ ] 数据需求（数据源/字段/更新频率/敏感级别）
    - [ ] 接口需求（输入输出、错误码、幂等、版本）
    - [ ] 业务流程与异常流程（含降级/回退）
    - [ ] 观测需求（日志/指标/追踪/审计）
    - [ ] 运维需求（部署、升级、回滚、容量、告警）

  - [ ] 非功能要求（通用）
    - [ ] 安全（鉴权/鉴别/最小权限/密钥/审计/隔离）
    - [ ] 可靠性（容错/重试/幂等/限流/熔断/降级）
    - [ ] 可用性（SLA/SLO、故障面、灰度、回滚）
    - [ ] 可扩展性（水平扩展、分片、缓存、异步化）
    - [ ] 性能（QPS、p95/p99、吞吐、并发、队列积压）
    - [ ] 成本（token/存储/带宽/算力、成本上限与预算）

  - [ ] 测试与验证（通用）
    - [ ] 单元测试（核心逻辑、边界、错误路径）
    - [ ] 集成测试（API、外部依赖、权限）
    - [ ] 可靠性测试（故障注入、超时、重试风暴、限流）
    - [ ] 性能压测（基线、峰值、退化点、容量曲线）
    - [ ] 安全测试（越权、注入、敏感信息泄露、审计）
    - [ ] 回归与验收（验收用例、UAT脚本、验收报告）

- [ ] 模型 OpenAPI 解决方案（API-first）
  - [ ] OpenAPI 规格（Contract）
    - [ ] 定义资源与端点（/models /chat /embeddings /tools /files 等按需要）
    - [ ] 定义请求/响应 schema（含示例）
    - [ ] 定义鉴权方案（API Key/OAuth、scope）
    - [ ] 定义错误模型（错误码、可重试标识、trace_id）
    - [ ] 定义幂等键/重试语义（Idempotency-Key）
    - [ ] 定义版本策略（/v1、兼容性规则、弃用流程）

  - [ ] 需求分析（OpenAPI）
    - [ ] 支持的能力清单（对话/函数调用/流式/多模态/文件）
    - [ ] 输入输出限制（大小、类型、超时）
    - [ ] 速率限制与配额（按 key/租户/模型）
    - [ ] 多租户隔离（租户ID、资源配额、审计）
    - [ ] 数据治理（日志留存、脱敏、数据出境/驻留）

  - [ ] 安全（OpenAPI）
    - [ ] 传输安全（TLS、证书、HSTS）
    - [ ] 身份与访问控制（RBAC、最小权限、scope）
    - [ ] 秘钥管理（轮换、KMS、密钥泄露处置）
    - [ ] 输入校验与防注入（JSON schema、长度限制、内容策略）
    - [ ] 审计与合规（请求日志、操作日志、可追溯）

  - [ ] 可靠性/可用性（OpenAPI）
    - [ ] 超时/重试策略（客户端与服务端一致性）
    - [ ] 幂等与去重（请求签名/幂等键/重放防护）
    - [ ] 限流/熔断/降级（按租户、按模型、按端点）
    - [ ] 灰度发布与回滚（版本路由、金丝雀）
    - [ ] 观测（trace_id贯穿、指标面板、告警阈值）

  - [ ] 可扩展性（OpenAPI）
    - [ ] 无状态化与水平扩展（API Gateway + stateless service）
    - [ ] 异步化（队列/任务、回调、轮询）
    - [ ] 缓存策略（响应缓存/embedding缓存/模板缓存）
    - [ ] 多区域与容灾（active-active/active-standby）
    - [ ] 成本控制（token预算、批处理、模型路由）

  - [ ] 测试验证（OpenAPI）
    - [ ] OpenAPI 合约测试（schema/兼容性）
    - [ ] 鉴权与越权测试（负例全覆盖）
    - [ ] 压测与容量评估（p95/p99、限流行为）
    - [ ] 故障演练（依赖超时、网关故障、降级生效）
    - [ ] 交付物：OpenAPI.yaml + Postman/Bruno Collection + 示例代码

- [ ] Agent 解决方案（能力到落地）
  - [ ] 行业/场景分析
    - [ ] 目标行业与业务链路梳理（上游/下游/系统边界）
    - [ ] 典型任务拆解（可自动化 vs 需要人工）
    - [ ] 关键约束（合规、数据敏感、审计、时延）
    - [ ] 竞品/替代方案对照（手工流程/规则引擎/RPA/现有平台）
    - [ ] 价值评估（节省工时、降低错误、提升响应速度）

  - [ ] 架构设计（Agent）
    - [ ] Agent 形态选择（单Agent/多Agent、Orchestrator/Worker）
    - [ ] 工具层设计（Tool registry、schema、权限、限额）
    - [ ] 记忆与上下文（短期/长期、向量库、可控留存）
    - [ ] 执行与状态机（工作流/图、重试、补偿、幂等）
    - [ ] 安全与隔离（工具权限、数据分级、审计日志）
    - [ ] 观测与评估（trace、decision log、质量指标）

  - [ ] 方案设计（Agent）
    - [ ] 任务输入输出规范（结构化输入、可解释输出）
    - [ ] Prompt/策略（系统提示、工具选择策略、拒答策略）
    - [ ] Guardrails（敏感信息、越权、注入、防幻觉机制）
    - [ ] 人在回路（审批点、确认点、回滚点）
    - [ ] 失败处理（超时、工具失败、部分成功、补偿事务）
    - [ ] 成本与性能（并发、批量、缓存、模型路由）

  - [ ] 方案验证与 Demo（Agent）
    - [ ] 选定一个“最小闭环”用例（输入→工具→输出→可验收）
    - [ ] 准备测试数据集（含正例/负例/边界）
    - [ ] 定义验收指标（正确率、人工介入率、时延、成本）
    - [ ] 实现 Demo（CLI 或 Web 最小界面）
    - [ ] 录屏与 README（一键运行、配置、示例）
    - [ ] 评估报告（失败案例、改进点、下一步路线图）